#include "entity.h"

#define DiaEntIndex(n) IDInd(DEnt_##n##)

u8  DialougEntList[]={
    DiaEntIndex(Pochanargat),//unlisted in appearance getters
    DiaEntIndex(Pillar),//unlisted in appearance getters
    DiaEntIndex(Flock),//shown as a horse, for some reason.
    DiaEntIndex(Fish),//shown as a horse, for some reason.
    DiaEntIndex(Exponto),
    DiaEntIndex(JundarSign),
    DiaEntIndex(TerminorSign),
    DiaEntIndex(MirariSign),
    DiaEntIndex(KingdomSign),
    DiaEntIndex(Bodecia),//unlisted in appearance getters
    DiaEntIndex(Baldric),
    DiaEntIndex(Jundargate),
    DiaEntIndex(Gillek),
    DiaEntIndex(Swagma),
    DiaEntIndex(Kolokos),
    DiaEntIndex(Horse),
    DiaEntIndex(Chicken),
    DiaEntIndex(Cow),//shown as a horse, for some reason.
    DiaEntIndex(Batrin),
    DiaEntIndex(Xibid),
    DiaEntIndex(Tamberlain),//unlisted in appearance getters
    DiaEntIndex(Niala),
    DiaEntIndex(Meriona),
    DiaEntIndex(Mago),
    DiaEntIndex(Lycea),
    DiaEntIndex(Herne),
    DiaEntIndex(Honza),
    DiaEntIndex(Gavinn),
    DiaEntIndex(Fyrsil),
    DiaEntIndex(Esmerelda),
    DiaEntIndex(Devlin),
    DiaEntIndex(Darmath),
    DiaEntIndex(Damek),
    DiaEntIndex(Cedric),
    DiaEntIndex(Cadme),
    DiaEntIndex(Balfin),
    DiaEntIndex(Amann),
};